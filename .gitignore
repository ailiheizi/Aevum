# Rust 构建产物
/target
/crates/*/target

# vendored 依赖(由 `cargo vendor vendor` 生成,体积大,不入版本控制)
# 重新生成:在能联网的环境跑 `cargo vendor vendor`。
# 离线构建按需启用:`cp .cargo/config.offline.toml .cargo/config.toml`(见该文件注释)。
/vendor

# WSL/Linux 侧独立 target
*.rs.bk

# Aevum 运行期状态(解包/store/世代/abi-bridge,由 prep 脚本与测试生成,可重建)
/.aevum

# VM 镜像与实验产物(大文件,不入库)
/vm/*.qcow2
/vm/*.img
/vm/*.iso
/vm/serial.log
/vm/boot.log
/vm/qemu-debug.log
/vm/vmlinuz
/vm/initramfs.cpio.gz
/vm/aevum-vm-key
/vm/aevum-vm-key.pub

# Nix 参考源码(开发参考,不入库)
/refs/

# .cargo/config.toml:本地构建配置(离线时 = config.offline.toml 的副本)。
# 不入库——committed 它会强制 vendor/ 致 clean clone/CI 编译失败(P1-2)。
# 在线构建模板见提交的 .cargo/config.offline.toml。
/.cargo/config.toml
/.cargo/config.toml.bak

# Claude Code / OMC 内部文件(不对外)
CLAUDE.md
.omc/
.claude/
